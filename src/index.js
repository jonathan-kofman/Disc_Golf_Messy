import React from 'react';
import ReactDOM from 'react-dom/client';
import DiscGolfTracker from './DiscGolfTracker';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <DiscGolfTracker />
  </React.StrictMode>
);
